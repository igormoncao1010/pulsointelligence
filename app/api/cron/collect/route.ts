import { collectActiveRssSources } from '@/services/collectors/rssCollector';
export const runtime='nodejs'; export const dynamic='force-dynamic';
export async function GET(request:Request){ const secret=process.env.CRON_SECRET; const auth=request.headers.get('authorization'); if(!secret||auth!==`Bearer ${secret}`) return Response.json({error:'Não autorizado'},{status:401}); try{const url=new URL(request.url);const batch=Number(url.searchParams.get('batch')??0);const totalBatches=Number(url.searchParams.get('total')??1);const results=await collectActiveRssSources({batch:Number.isInteger(batch)?batch:0,totalBatches:Number.isInteger(totalBatches)?totalBatches:1});return Response.json({ok:true,batch,totalBatches,executedAt:new Date().toISOString(),results});}catch(error){console.error('[cron/collect]',error);return Response.json({ok:false,error:error instanceof Error?error.message:'Falha na coleta'},{status:500});} }
export const POST=GET;
